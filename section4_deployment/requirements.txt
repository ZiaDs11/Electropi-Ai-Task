# Serving + load test (enough for the mock backend and the Docker image)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
aiohttp>=3.9.0

# Uncomment for the real model backend (MODEL_BACKEND=hf):
# torch>=2.2.0
# transformers>=4.44.0
# accelerate>=0.33.0
